# Database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/school_management_system

# JWT
JWT_SECRET=your-secret-key-change-in-production
JWT_EXPIRES=1d

# Server
PORT=5000

# --- Seeder (prisma db seed) ---
# Super Admin credentials – used by prisma/seed.js
SUPER_ADMIN_USER=superadmin
SUPER_ADMIN_PASS=your-secure-password
SUPER_ADMIN_EMAIL=superadmin@school.local

# Optional: run in development mode (seeds subjects + classes)
NODE_ENV=development

# Optional: clear dev tables before seeding (local testing only)
# SEED_CLEANUP=true
